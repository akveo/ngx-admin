sudo: false

language: node_js

node_js:
  - '8'

cache:
  directories:
    - node_modules

before_install: if [[ `npm -v` != 5* ]]; then npm i -g npm@5; fi

install:
  - npm i

script:
  - npm run lint:ci
  - npm run build:ci

git:
  depth: 1

